namespace TridevLoader.Core {
    /// <summary>
    /// Contains static hooks that the game can call, fires events and loads config values that modify the game's behaviour.
    /// </summary>
    public class TridevHooks {
    }
}